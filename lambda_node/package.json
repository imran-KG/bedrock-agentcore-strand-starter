{
  "name": "bedrock-agentcore-proxy",
  "version": "1.0.0",
  "description": "Lambda proxy for Bedrock Agent Core Runtime",
  "main": "index.js",
  "scripts": {
    "build": "tsc",
    "package": "npm run build && cd dist && zip -r ../lambda_deployment_node.zip ."
  },
  "dependencies": {
    "@aws-sdk/client-bedrock-agentcore": "^3.0.0",
    "@types/aws-lambda": "^8.10.145"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "@types/node": "^22.10.2"
  }
}
